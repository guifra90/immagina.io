export default function Loading() {
    return (
        <div className="fixed inset-0 z-[999999] bg-[#111111] flex items-center justify-center">
            {/* Optional: Minimal loader or branding if needed, but black Screen is safest for continuity */}
        </div>
    )
}
