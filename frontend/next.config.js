/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: [], // Aggiungi domini per immagini esterne se necessario
    },
}

module.exports = nextConfig
